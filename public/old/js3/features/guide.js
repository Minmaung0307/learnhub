export async function viewGuide(){ const app=document.getElementById('app'); app.innerHTML=''; app.insertAdjacentHTML('beforeend',`<h2>Guide</h2><div class=card><p>• Use Settings for theme & font.</p><p>• Admin → Set Me Admin, then Seed Demo.</p><p>• Courses: enroll free or PayPal for paid (demo).</p><p>• My Learning: progress auto-saves.</p><p>• Tasks: drag between lanes; Done purges after 2 days.</p><p>• Chat: real-time via Firestore.</p></div>`);}